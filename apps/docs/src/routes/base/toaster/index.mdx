import api from "./code-notate/api.json";

# Toaster Component

The Toaster component is a headless toast notification system built on top of the Qwik Design System's popover component. It provides a flexible and accessible way to display toast notifications in your application.
<Showcase name="hero" />
## Features
<Features api={api} />
## Anatomy
<AnatomyTable api={api} />
