import api from "./code-notate/api.json";

# Date Input
A segmented input field for selecting dates with keyboard navigation and format customization.

<Showcase name="temp" />

<Showcase name="composed" />